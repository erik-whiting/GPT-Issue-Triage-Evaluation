from github_utils import github_methods as gm
from prompts import gpt_methods_embeddings as gpt_emb


model_under_test = "With-Code-Embeddings"
nuitka_issue = 20

repo = gm.get_repo()
issue = repo.get_issue(nuitka_issue)

issue_text = gm.parse_issue(issue)

model = gpt_emb.generate_model(issue_text)
response = model.choices[0].message.content
response += f"\n\n{'-'*50}\n\n"
response += f"This test-response generated by model version *{model_under_test}*"
issue.create_comment(response)

print("Done")
